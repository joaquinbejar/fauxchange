/target
/.claude
/docs
/rules
/AGENTS.md
/CLAUDE.md
/milestones
# Local environment secrets (never commit; use .env.example as the template)
.env

# JetBrains IDE metadata (local; must not ship)
/.idea/

# dhat heap-profile output from `benches/alloc_dhat.rs --features dhat-heap`
# (#126); a generated per-run artifact, never committed.
/dhat-alloc.json
/dhat-heap.json
